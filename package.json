{
  "name": "corporatefilter-ai",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vercel dev --listen 3000",
    "deploy": "vercel deploy --prod"
  }
}
